# Model events separately while sharing the call surface

Model an Event separately from a Coffee-chat booking because events have publishing, registration, capacity and payment lifecycles that coffee chats do not. Both enter the existing LiveKit call surface, with an explicit small-call or event mode, so the mature media flow is extended rather than duplicated and the eight-person coffee-chat path does not inherit event pagination or bounded video subscriptions.
